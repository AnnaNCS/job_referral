#include "graph_node.h"
#include "pair.h"
#include "vector.h"
#include <string.h>
#include <stdlib.h>

void pair_inti(char* name){

    int len = strlen(name);
    char* name_init = (char*) malloc (sizeof(char) * len);

}